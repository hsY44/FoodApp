<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature android:name="android.hardware.camera"
        android:required="true" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SafeFood"
        tools:targetApi="31">
        <activity android:name=".SubActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".CameraActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".GalleryActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Join"/>
        <activity android:name=".HistoryActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".MyPageActivity"
            android:screenOrientation="portrait"/>

        <!-- CameraActivity에서 카메라 앱과 이미지 파일을 안전하게 공유하기 위한 FileProvider -->
        <!-- authorities는 패키지명과 일치해야 함 - FileProvider.getUriForFile() 두 번째 인자와 동일 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="kr.ac.baekseok.ab"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>